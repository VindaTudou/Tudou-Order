<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.fangtudou.mapper.OrderDetailMapper">

    <insert id="insertBatch" parameterType="list">
        insert into order_detail (name, order_id, dish_id, setmeal_id, dish_flavor, number, amount, image)
        values
        <foreach collection="list" item="d" separator=",">
            (#{d.name}, #{d.orderId}, #{d.dishId}, #{d.setmealId}, #{d.dishFlavor}, #{d.number}, #{d.amount}, #{d.image})
        </foreach>
    </insert>

    <select id="getByOrderId" resultType="io.fangtudou.entity.OrderDetail">
        select * from order_detail where order_id = #{orderId}
    </select>

</mapper>